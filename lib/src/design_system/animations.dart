class AppAnimations {
  static const Duration short = Duration(milliseconds: 500);
  static const Duration medium = Duration(milliseconds: 800);
  static const Duration long = Duration(milliseconds: 1500);

  static const Duration fadeInDuration = Duration(milliseconds: 600);
  static const Duration scaleDuration = Duration(milliseconds: 800);

  static const Duration delay200 = Duration(milliseconds: 200);
  static const Duration delay400 = Duration(milliseconds: 400);
  static const Duration delay600 = Duration(milliseconds: 600);
  static const Duration delay800 = Duration(milliseconds: 800);

  static const double slideOffset = 0.2;
  static const double slideOffsetNegative = -0.2;
  static const double shimmerBegin = -2.0;
  static const double shimmerEnd = 2.0;
  static const double shimmerStop = 0.3;
}
